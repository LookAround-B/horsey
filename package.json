{
  "name": "horsey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter shared build && pnpm -r --parallel --filter=!shared dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter shared build && pnpm --filter api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}